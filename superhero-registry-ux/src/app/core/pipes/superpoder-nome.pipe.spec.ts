import { SuperpoderNomePipe } from './superpoder-nome.pipe';

describe('SuperpoderNomePipe', () => {
  it('create an instance', () => {
    const pipe = new SuperpoderNomePipe();
    expect(pipe).toBeTruthy();
  });
});
